# Bug hunt 2026-07-11 — 6-dimension sweep + adversarial verify (11 confirmed / 2 refuted)

19 agents; every finding below survived an adversarial refute pass with code-level verification.

## [HIGH] src/engine/cssRender.ts:145 — signalsCss dark block loses to its own later P3 light block — light display-p3 values leak into dark mode on root-level themed pages

**Mechanism:** signalsCss emits dark base under bare `[data-theme="dark"]` (specificity 0,1,0) but the P3 light overrides LATER under `:root` (also 0,1,0). On an element matched by both — html carrying data-theme="dark", the standard `html[data-theme]` toggle for the shipped dist/signals.css — cascade order wins ties, so every token present in the P3 light block but absent from the P3 dark block resolves to its LIGHT display-p3 value in dark mode. Verified against generated output (shipped apca lane): 11 tokens hit — --red-wash-3..6 (dark base #241511 near-black -> color(display-p3 0.9895 0.9200 0.8984) near-white: a blinding light wash inside dark UI), --yellow-paper-1/2, --yellow-wash-3, --yellow-highlight-9, --yellow-ink-10/11 (light ink values = dark text on dark bg, yellow-signal text unreadable), --green-highlight-9. Hue-specific: only stops whose light rendition is out-of-sRGB while the dark one is in-gamut. brandCss and neutralCss are immune because their dark selectors compound the base selector ([data-brand="x"][data-theme="dark"], 0,2,0 > the 0,1,0 P3 light block) — signalsCss is the lone emitter that breaks the pattern. Demo unaffected (data-theme rides divs, so the div's own dark declarations beat the inherited :root P3 values), which is why it hasn't been seen. Same exact class as the owner-caught outline-P3 pop: a later @supports block re-overriding an earlier mode re-resolution.

**Repro:** <html data-theme="dark"> + dist/signals.css, P3 display, any browser supporting color(display-p3) [Safari/Chrome]. Inspect --red-wash-3: computes to the near-white light P3 value, not #241511. Programmatic proof: signalsCss('apca'), diff var names in the @supports :root block vs the @supports [data-theme=dark] block -> 11 light-only vars, each with equal-specificity later-in-file precedence over the dark base.

**Verify:** NOT refuted — claim confirmed end-to-end. (1) Code: src/engine/cssRender.ts signalsCss emits base dark under bare `[data-theme="dark"]` (line 145, specificity 0,1,0), then P3 light under bare `:root` (line 151, also 0,1,0) LATER in file; @supports/@media wrappers don't affect cascade precedence, so on <html data-theme="dark"> (matches both selectors) source order breaks the tie and the P3 light declaration wins for any var absent from the P3 dark block. (2) Programmatic proof: ran signalsCss('apca') — exactly 11 light-only P3 vars, matching claim list verbatim: red-wash-3..6, yellow-paper-1/2, yellow-wash-3, yellow-highlight-9, yellow-ink-10/11, green-highlight-9; e.g. --red-wash-3 dark base #241511 (near-black) loses to later color(display-p3 0.9895 0.9200 0.8984) (near-white). (3) Shipped artifact: src/build.ts SHIPPED_PROFILE='apca' writes dist/signals.css; inspected file — base dark --red-wash-3 at line 71, P3 :root re-declares it at line 137, P3 dark block (line 155) omits it. (4) Immunity contrast holds: brandCss/neutralCss dark selectors are compound (0,2,0) and beat the later 0,1,0 P3 light block on specificity; signalsCss is the lone flat-selector emitter. (5) Why unseen: demo puts data-theme on divs (demo/App.tsx:33, shared.tsx:442, CustomTheme.tsx:1257) — the div's own matched dark declarations beat inherited :root P3 values. Not intended behavior: same bug class as the owner-caught outline P3 pop documented at cssRender.ts:274-278. Manifests only on P3 displays in browsers supporting color(display-p3), per the @media/@supports gates — exactly as claimed.

## [MEDIUM] src/engine/resolve.ts:214 — redRepel hardcodes dark:false — dark cta exits (solveDarkCtaExit) never reported, dark-only movers report null

**Mechanism:** Line 214: `const redRepel = scale.ctaRepelled?.light ? { light: true, dark: false } : null`. Predates the dark-side solve (858053e): generateScale now sets ctaRepelled.dark=true when solveDarkCtaExit fires (apca-lane near-reds lift ~0.70->0.77), but resolveBrand's reported flag (a) always claims dark:false, (b) returns null entirely when only dark fired. Measured: 90/90 apca-lane dark-fire seeds misreported dark:false; 17/90 fired dark WITHOUT light -> redRepel=null although the shipped dark cta moved. Wrong data flows to cssRender.ts:163 ('conflict with red resolved' note absent for dark-only movers), src/build.ts:30 note, demo/shared.tsx:393 chip (renders per-mode 'light+dark' string that can never show dark), and CustomTheme.tsx remediation panels (line 460 branch treats a dark-only mover as 'no repel, variant only'). The color output itself is correct; the per-mode audit/annotation contract of ResolvedBrand.redRepel ('per-mode fired flags') is provably violated.

**Repro:** resolveBrand('#c20017','x',{contrastProfile:'apca'}) -> scale.ctaRepelled={light:true,dark:true} but redRepel={light:true,dark:false}. Dark-only: sweep h10-45, L .5-.75, C .14-.22 apca lane -> 17 seeds with ctaRepelled={light:false,dark:true} and redRepel===null (probe: scratchpad/flagprobe3.ts).

**Verify:** Claim CONFIRMED, not refuted. (1) Mechanism real: src/reqtoken/resolve.ts:407 sets cta.repelled=true when solveDarkCtaExit fires; colorEngine.ts:200 propagates to ctaRepelled.dark; but src/engine/resolve.ts:214 reads only ctaRepelled.light, hardcodes dark:false, and returns null when only dark fired — while the type contract at resolve.ts:56-59 declares redRepel "per-mode fired flags". (2) Not intended: git log -L shows line 214 written at C12 v6 (pre-dark-exit) and untouched by dark-solve commit 858053e (which changed resolve.ts only to inject ctaSolve.redDark); producers.ts comment says 858053e supersedes the v6 "no dark exit" ruling; no doc/session record declares light-only reporting deliberate. (3) Repro holds: resolveBrand('#c20017','x',{contrastProfile:'apca'}) → ctaRepelled={light:true,dark:true} but redRepel={light:true,dark:false}; apca sweep h10-45/L.5-.75/C.14-.22 (144 seeds): 26 dark-fires all misreported dark!==true, 5 dark-only fires all returned redRepel=null despite shipped ctaDark moving 0.700→0.777 vs exact (the exact lift 858053e's commit message documents). Consumers (cssRender.ts:163, build.ts:30, demo/shared.tsx:393 — its 'dark' string branch is dead code — and CustomTheme.tsx:460) all read the wrong flag. Color output unaffected; the per-mode audit/annotation contract is provably violated.

## [MEDIUM] plugin/ui.ts:239 — Ink renumber missed both plugins' chip-tone block — stop-12 lookup throws every preview render

**Mechanism:** Commit 1aae676 (ink-11→10, 12→11, 13→12; scale now ends at stop 11) migrated renderMatrix in both plugins but NOT the chip TONES block. plugin/ui.ts:239 `hxs(at(t.themed.scale.light, 12))` and :249 `at(sl, 12)` look up a stop that no longer exists; `at()` = `.find()!` returns undefined and `hxs` throws TypeError reading .r. Under the default primaryMode 'recommended' this throws on EVERY updatePreview, so the whole tail of the try block is dead: chip tints never apply and syncInfoLines() never runs — mode-chip labels and info copy stay frozen at the template defaults (picking Exact/an archetype never updates the label). Identical code in plugin-ext/ui.ts:248 and :258. Additionally the outline chip (plugin/ui.ts:247, plugin-ext/ui.ts:256) uses `at(sl, 11)` — pre-renumber ink-11 is now stop 10; it silently shows the near-black ink-11 (L≈0.30) instead of ink-10 (L≈0.53), diverging from demo/CustomTheme.tsx:209 (`--secondary-ink-10`) and from renderMatrix's own outline cells (st(10)). Demo migrated automatically via CSS vars (--brand-ink-11 etc.); the plugins' direct stop lookups did not. Fix: 12→11 (chip ink), outline 11→10.

**Repro:** Verified via real pipeline: `resolveTheme({primaryHex:'#E93D82',...}).themed.scale.light` stops = 1..11, find(stop===12) = undefined, hxs(at(...,12)) throws 'Cannot read properties of undefined (reading r)'. In the plugin: open either plugin, watch console — 'okchroma preview render failed: TypeError...' on every keystroke; switch the primary mode select to Exact — chip label stays 'Recommended'.

**Verify:** CONFIRMED, not refuted. Verified via real pipeline: resolveTheme({primaryHex:'#E93D82'}).themed.scale.light stops = 1..11 (commit 1aae676's own message says new ink-12 = the off-scale #000/#fff anchor, not a resolved stop), so at(scale,12)=undefined and hxs throws exactly 'Cannot read properties of undefined (reading r)' — reproduced live. git show 1aae676 confirms plugin/ui.ts and plugin-ext/ui.ts diffs migrated only renderMatrix (st(11)→st(10), n>=11→n>=10) and left the chip TONES block untouched: plugin/ui.ts:239 at(light,12), :249 at(sl,12), :247 at(sl,11); same in plugin-ext/ui.ts:248/:258/:256. Default primaryMode='recommended' (ui.ts:16) takes the throwing branch on every updatePreview (initial :472 + all handlers), and syncInfoLines() is called only at :252 after the chip block, so info/label sync is dead on that path. Outline divergence also real: post-renumber stop 11 = old ink-12 near-black #431928 vs intended stop 10 #993e5f; demo/CustomTheme.tsx:209 and renderMatrix both use ink-10. Two minor imprecisions that don't refute: primary chip background (stop 4, :238) applies before the throw; and the 'switch to Exact — label stays Recommended' repro detail is wrong in the default secondary-off state (exact + no secondary skips all stop-12 lookups, syncInfoLines runs, label updates) — the frozen-label symptom holds for recommended/archetype modes or whenever a secondary exists.

## [HIGH] plugin/ui.ts:300 — C12 red-variant note format breaks v1's shared-primitive dedup key — colliding brands silently share the wrong red cta

**Mechanism:** v1 dedup-keys shared signal primitives by parsing the override note: `variant = ov.note.split('→').pop()!.trim().toLowerCase().replace(/\s+/g,'-')` → path `system/red/<variant>`. That contract assumed the note uniquely identifies the ramp (old closed set: lemon, teal-side, ...). C12 v8 (resolve.ts:140) changed the red note to `red → rich|coral L<2dp>` and deliberately dropped the hue suffix ('No hue suffix: the plugin's note parser mints Figma variable paths from this string'), but redComplementVariant picks BOTH L and H from candidate grids (colorMath.ts:148-149 coreL/edgeL × redHuesWarmBrand/redHuesMagentaBrand) — different brands land the SAME rounded L with DIFFERENT hue. plugin/code.ts writes shared primitives with refresh=false (line 339: 'only consulted the first time'), so the second brand's theme mode aliases the FIRST brand's variant values: it ships a red cta whose hue was solved to sit clean beside a different brand — exactly the vibration C12 exists to prevent, silently. Secondary risk: the minted leaf ('rich-l0.49') now contains a period in a Figma variable name (unverified whether createVariable accepts '.'). Fix: key the variant by its resolved cta values (or include H), not by the display note.

**Repro:** Proven: resolveBrand('#EA603E') → red variant cta H28.0, note 'red → rich L0.49'; resolveBrand('#E91E63') → H33.3, same note ⇒ same key 'rich-l0.49'. Also '#A50034' (roster cranberry) H33.3 vs '#D81B60' H38.0 ⇒ both 'coral-l0.65'. In v1: apply #EA603E, then #E91E63 to the same file — the second brand's red/cta-1 aliases system/red/rich-l0.49 carrying #EA603E's H28 values.

**Verify:** CONFIRMED, not refuted. Mechanism verified: plugin/ui.ts:300 keys shared signal prims by note RHS ('rich-l0.49'), src/engine/resolve.ts:140 mints hue-less notes, redComplementVariant picks H per-brand from grids (colorMath.ts:148-149) so same L + different H produce identical keys, and plugin/code.ts writeRaw refresh=false (line 339, 314) silently reuses the first brand's values — second brand's theme aliases a red cta solved beside a different brand. Repro reproduced exactly by running the engine: wcag lane #EA603E H28.0 vs #E91E63 H33.3 both 'rich-l0.49'; #A50034 H33.3 vs #D81B60 H38.0 both 'coral-l0.65'; the shipped apca default lane also collides (#EA603E H28.0 vs #EC407A H33.3, both 'rich-l0.49'). Not intended: ui.ts's own neutral key includes hue for exactly this reason, and scripts/c12-session/HANDOFF.md:72-73 records the collision as an open panel-finding follow-up, not accepted behavior. Only the '.'-in-Figma-variable-name side note remains unverified (needs live Figma).

## [MEDIUM] plugin/code.ts:370 — staleInk migration renames the anchor onto a still-occupied name — legacy files get the anchor hijacked into scale ink-11

**Mechanism:** The pre-pass (lines 370-375) renames the oldest-layout theme anchor `system/ink-13` DIRECTLY to `neutral/ink-12` — but on a pre-renumber file that name is still held by the not-yet-migrated old SCALE ink-12 variable. `themeByName.set('neutral/ink-12', staleInk)` clobbers the scale variable's map entry. The neutral alias loop then migrates new ink-11 via RENAMED_LEAVES ['ink-12','ink-11'] → `map.get('neutral/ink-12')` returns the ANCHOR, renames it to `neutral/ink-11` and aliases it to the scale ink-11 primitive: every user binding to the anchor (a mode-flipping #000/#fff pole) silently resolves to the brand-tinted ink-11 color. The real old scale ink-12 variable is left orphaned (still named neutral/ink-12, duplicate with the fresh anchor variable created at the `aliasInto('neutral/ink-12','system/ink-12')` step). This violates the ascending-order safety the same commit (1aae676) documents ('any future renumber must keep that ascending order') — the pre-pass jumps the anchor to its final name before the scale migration frees it. Files on the intermediate layout (anchor already neutral/ink-13, post-3b04e64) migrate correctly; only oldest-layout files (anchor at theme system/ink-13) corrupt. Fix: rename staleInk to 'neutral/ink-13' and let the RENAMED_LEAVES ['ink-13','ink-12'] entry finish the move in ladder order (the code comment already claims that path handles it).

**Repro:** File with theme vars from the pre-3b04e64 plugin: neutral/ink-11, neutral/ink-12 (scale), system/ink-13 (anchor, user-bound). Re-apply any brand with current plugin: step through — staleInk sets map['neutral/ink-12']=anchor; alias loop ink-10←old ink-11 (ok); ink-11's legacy lookup 'neutral/ink-12' returns the anchor → anchor.name='neutral/ink-11', aliased to scale ink-11 prim; then a NEW neutral/ink-12 is created while the old scale ink-12 var still carries that name.

**Verify:** CONFIRMED, not refuted. plugin/code.ts:370-375 pre-pass renames the oldest-layout anchor system/ink-13 directly to 'neutral/ink-12' and set()s it into themeByName, clobbering the map entry of the not-yet-migrated old scale ink-12 variable (pre-renumber files verifiably hold neutral/ink-11 + neutral/ink-12 scale vars — that is what RENAMED_LEAVES ['ink-11','ink-10']/['ink-12','ink-11'] exist for; git show 3b04e64 confirms system/ink-13 was the pre-Jul-4 anchor home and 1aae676 changed the pre-pass target from neutral/ink-13 to neutral/ink-12). Step-through holds: ascending neutral loop migrates ink-10←old ink-11 (deleting the 'neutral/ink-11' key), then ink-11's direct get misses and the legacy get('neutral/ink-12') returns the ANCHOR → renamed neutral/ink-11 and aliased to the brand-tinted scale ink-11 primitive, hijacking every user binding on the mode-flipping #000/#fff pole; the anchor aliasInto then creates a fresh neutral/ink-12 while the orphaned old scale var still carries that name (duplicate). Intermediate layout (neutral/ink-13 anchor) is indeed safe — pre-pass no-op, ladder order handles it. Behavior contradicts the same commit's documented ascending-order invariant, so not intended. Proposed fix (pre-pass → 'neutral/ink-13', let the ['ink-13','ink-12'] entry finish in ladder order) is sound.

## [MEDIUM] demo/shared.tsx:111 — Focus-visible ring dead in app-chrome scope — keyboard focus indicator lost on footer controls

**Mechanism:** COMPONENT_CSS rule `[data-brand] :is(input, select, textarea, button, a):focus-visible { outline: 2px solid var(--brand-highlight-8) }` is injected at App root and matches the shell div `data-brand="chrome"` (App.tsx:33). But chrome's scope only gets neutralCss output (App.tsx:37) — `--neutral-*`, `--paper-0`, `--ink-12`; NO `--brand-*` vars exist there (dist/brands.css defines them only under real brand slugs; verified). var() substitution fails -> declaration invalid at computed-value time -> outline longhands go to unset -> outline-style: none. Author rule still wins cascade over UA default, so footer view-switcher buttons, GitHub pill, dark toggle, and Figma link show NO focus indicator at all for keyboard users. Inner scopes (data-brand=custom) are fine.

**Repro:** Load demo, Tab to the footer 'Custom theme'/'Documentation' buttons or the dark-toggle pill -> no visible focus ring (and no UA fallback ring either). Tab into the Custom-theme page controls -> ring appears (brand vars defined there).

**Verify:** Claim CONFIRMED, not refuted. (1) demo/shared.tsx:110-113 has the exact rule with fallback-less var(--brand-highlight-8). (2) demo/App.tsx:33 shell div is data-brand="chrome" and [data-brand] matches it; footer controls (App.tsx:49-67) are its direct descendants, outside every inner brand scope. (3) App.tsx:37 gives chrome only neutralCss output; src/engine/cssRender.ts:90-119 shows neutralCss emits solely --paper-0/--ink-12/--neutral-* — no --brand-* primitives. (4) No definition of --brand-highlight-8 exists anywhere reachable by the chrome scope: dist/brands.css scopes exclusively under [data-brand="<slug>"] (0 chrome hits, 0 :root blocks), dist/signals.css 0 hits, tokens/semantic.css only references the primitive (line 56) and never defines it; repo-wide grep for '--brand-highlight-8:' (definition form) returns zero hits. (5) No alternate focus indicator: NAV_CSS has no focus rules; CustomTheme's lone focus-visible rule is .ct-field-scoped (and sets outline:none). (6) CSS mechanism is correct per spec: unresolvable var() in the outline shorthand → invalid at computed-value time → longhands unset → outline-style:none; author-origin declaration still wins cascade over the UA :focus-visible ring (cascade precedes IACVT substitution), so no UA fallback either. (7) dist/demo.js contains both the rule and the chrome scope, so the shipped demo matches source. Inner scopes (e.g. data-brand="custom") define the primitive via brandCss, matching the claim's "inner scopes fine". The comment above the rule ("Stop 8 IS the ramp's focus-ring role") shows a visible ring is intended, ruling out intended-behavior refutation.

## [MEDIUM] demo/shared.tsx:406 — Gallery view built on pre-rename dead tokens — ScaleStrip numeric stops, phantom 'accent' prefix, numeric neutrals

**Mechanism:** ScaleStrip renders `var(--${prefix}-${n})` for n=1..12, but the engine emits only NAMED tokens (paper-1/2, wash-3..7, highlight-8/9, ink-10/11 — tokenNames.ts; zero `--brand-1:`-style vars in dist/brands.css, verified by grep). There is also no stop 12 in the ramp. Showcase additionally calls ScaleStrip with prefix="accent" (shared.tsx:248) — secondary primitives emit as `--secondary-*`, `--accent-*` never exists. Same rot in App.tsx gallery controls: `--neutral-6` (App.tsx:79, GX_FIELD border), `--neutral-11` (App.tsx:101), `--neutral-8` (App.tsx:105) — pre-generated-neutral numeric names, absent from every emitted sheet (comm diff of used-vs-emitted vars). And the Typography section prose (shared.tsx:365-368) still says 'neutral-12'/'brand-11'/'neutral-11' — post ink-renumber the real registers are neutral-ink-11 / brand-ink-10 / neutral-ink-10. The gallery is currently commented out of VIEWS (App.tsx:21) with a comment promising a 'one-line revert' — re-enabling ships three all-transparent 12-box scale strips, borderless locked fields, and wrong token documentation.

**Repro:** Uncomment `['gallery', 'Example palettes']` in App.tsx VIEWS -> Scales section renders empty bordered boxes for all three strips (background: var(--brand-1) etc. resolves to nothing); locked-field borders vanish (border shorthand invalid at computed-value time).

**Verify:** Claim verified, not refuted. (1) demo/shared.tsx:406-407 ScaleStrip renders var(--{prefix}-{n}) n=1..12; engine vocabulary is named-only — src/engine/tokenNames.ts stopTokenName maps 1-11 to paper/wash/highlight/ink names and THROWS on 12; cssRender.ts brandKindBody emits only those names; grep dist/brands.css shows 0 numeric --brand-N/--neutral-N and 0 --accent-* vars. (2) shared.tsx:248 passes prefix="accent" but secondary primitives emit as --secondary-* (prim() mapping at shared.tsx:30 documents this); --accent-1..12 never exists anywhere. (3) App.tsx:79/101/105 (also 103) use --neutral-6/-11/-8; repo-wide grep finds zero definitions of numeric neutral vars in demo/ or src/; the only numerics live in dist/neutral.css, a stale pre-rename artifact the current build no longer writes (src/build.ts emits only signals.css+brands.css) and demo/index.html never links. (4) Typography prose at shared.tsx:365-368 does cite neutral-12/brand-11/neutral-11 vs the real post-renumber registers (fg-link is brand-ink-10 per shared.tsx:50). (5) Gallery is commented out at App.tsx:21 with a "one-line revert" comment; Showcase's only consumer is PaletteGallery, so the bug is latent and fires exactly on the promised revert. (6) Repro mechanism is correct CSS: unresolved custom property → invalid at computed-value time → background/border shorthand unset → transparent 12-box strips with only the --border-subtle box borders, and vanished LockedField borders. Only quibble: stale dist/neutral.css technically contains numeric neutrals, but it is neither generated nor loaded, so it cannot resolve the vars.

## [MEDIUM] demo/App.tsx:123 — Gallery Exact toggle silently swaps contrast lane apca->wcag (and leaves signals mixed)

**Mechanism:** Recommended mode shows the prebuilt CSS (built with SHIPPED_PROFILE='apca', src/build.ts). The exact recompute calls `resolveBrand(current.hex, name, { exact: true })` and `brandCss(slug, name, r, accent)` with NO contrastProfile -> everything re-solves under the default wcag lane. Measured divergence: generateNeutralScale wcag-vs-apca differs at 24/24 hues — e.g. h=0 light highlight-8 #968a8d vs #b1a7a9, ink-10 #73696b vs #685e60, dark highlight-9 #7f7376 vs #b8abaf, and `--neutral-on-highlight` POLE FLIPS in both modes. So toggling Recommended<->Exact changes far more than 'skip adjustments': the whole neutral + on-text solve jumps lanes, while dist/signals.css (apca) stays — a mixed-lane page. Secondary accent resolved `{ exact: true }` likewise drops the lane. Gallery is hidden today, same one-line-revert exposure as above. Fix shape: thread the shipped/selected profile into both resolveBrand and brandCss like CustomTheme.tsx:144-159 does.

**Repro:** Re-enable gallery, pick any brand, toggle Recommended->Exact -> neutral highlight/ink stops and on-highlight polarity shift for reasons unrelated to exactness (brand cta poles verified identical across lanes in exact mode — the visible drift is entirely the lane swap).

**Verify:** Claim CONFIRMED, not refuted. (1) Recommended mode shows prebuilt apca CSS: demo/index.html:8-9 links dist/signals.css + dist/brands.css, built with SHIPPED_PROFILE='apca' threaded into resolveBrand/brandCss/signalsCss (src/build.ts:11,18,27,33,46); gallery recommended returns overrideCss:'' (App.tsx:124). (2) Exact toggle recomputes with NO contrastProfile: App.tsx:122-126 calls resolveBrand({exact:true}) for primary and accent and brandCss with 4 args (contrastProfile is param 7, cssRender.ts:186) — engine defaults undefined to wcag (colorEngine.ts:166 '?? wcag', onEnforce/onFloor branches at 263-264/320-321; signalScalesFor returns wcag SIGNAL_SCALES for undefined, resolve.ts:41). (3) Divergence reproduced through the real engine (esbuild+node probe): generateNeutralScale wcag-vs-apca differs at 24/24 hues; h=0 values match claim's cited hexes within rounding (hl-8 #948a8d vs #afa7a9, ink-10 #71696b vs #665e60, dark hl-9 #7d7476 vs #b6acaf); cold-brew exact brandCss differs 15/128 lines with --neutral-on-highlight pole-flipping #000<->#fff in both modes. (4) Mixed lane confirmed: gallery emits no signalsCss override, dist apca signals remain. (5) Fix shape accurate: CustomTheme.tsx threads cp into resolveTheme/brandCss/signalsCss and its own comment calls a profile-less resolve a bug it fixed. Only counterpoint — gallery unreachable today (App.tsx:21 commented out of VIEWS) — is explicitly disclosed in the claim itself ('hidden today, one-line-revert exposure'), so it does not make the claim wrong.

## [HIGH] scripts/dark-audit.ts:128 — Check E 'dark error collision' uses redGateDist (P1) — the metric this repo documents as blind to dark vibration

**Mechanism:** E asserts `redGateDist(scale.ctaDark, err.scale.ctaDark) <= RED_GATE.G` (imports at line 19 pull only redGateDist; p2Diff never imported). collision-sweep.ts:96-98 states in-repo: 'the P1 gate passes vibrating dark pairs (redGateDist 0.11-0.20 while p2 sits 0.086-0.109 — the dark blindness)' and its own dark assertion (line 100/119) was switched to p2Diff >= P2_D_UP for exactly this reason. So the fleet-level (66 brands+signals) dark check still measures with the blind instrument: the C12 F1/F2 defect class — 12 shipped brands vibrating beside red dark at p2 0.086-0.109 — reads '0 failures' under E. A regression in solveDarkCtaExit (producers.ts:562) re-shipping dark vibration is invisible here; collision-sweep only backstops synthetic seeds within ±35° of signal hues, not the real fleet. Fix: E dark side = p2Diff(ctaDark, redDark) >= P2_D_UP.

**Repro:** Stub solveDarkCtaExit to return null (or lower P2_D_UP consumption in resolve); run `npm run audit` — E stays 0 because redGateDist for the known vibrating class sits 0.11-0.20, above the gate; `npm run sweep:collision` catches only its own seed grid.

**Verify:** Claim verified, not refuted. dark-audit.ts:19 imports only redGateDist (no p2Diff anywhere in file); line 128 E asserts redGateDist(ctaDark, red ctaDark) <= RED_GATE.G (0.090, colorMath.ts). Repo documents the vibrating dark class at redGateDist 0.11-0.20 with p2 0.086-0.109 in three places (commit 858053e message, producers.ts:554-556, collision-sweep.ts:96-98) — all above 0.090, so E reads 0 failures for the exact C12 F1/F2 defect class. Not intended: commit 858053e switched collision-sweep's dark assertion to p2Diff>=P2_D_UP and shipped solveDarkCtaExit but its file list omits dark-audit.ts — E kept P1 by omission; E's comment now contradicts the repo's own dark-blindness docs. Repro sound: npm run audit = dark-audit only; its blessed snapshot records only ramp stops light/dark.slice(0,12) (cta is off-scale) so a stubbed solveDarkCtaExit leaves E at 0 with no snapshot drift. Fleet gap real: collision-sweep iterates a synthetic grid (±35°, C<=0.17, L 0.55-0.80), never BRANDS; real vibrating brands sit at higher C. Prior repo research (dark floor L0.70 vs red 0.63, 0/1206 fires) makes E structurally vacuous in dark. Only mitigation: sweep:collision (asserted, exit 1) and audit:ext snapshot drift would catch a wholesale regression on synthetic/ext coverage — but the claim already concedes this and its E-scoped statements are accurate.

## [HIGH] scripts/highlight-audit.ts:203 — Highlight snapshot drift-compare reads only L — C and H drift on the highlight rung and all four cta roles is invisible

**Mechanism:** rungAndCta (line 184-185) records [L,C,H] triples for light[8], dark[8], cta, ctaHover, ctaDark, ctaHoverDark, but the compare loop steps `i += 3` and tests only `v[i]` (L). Chroma and hue moves pass as 'clean — matches blessed'. This is the ONLY snapshot covering ctas under the SHIPPED apca profile (items resolved with contrastProfile:'apca', line 117/120) — dark-audit's snapshot is stops 1-11 only, no cta role at all (dark-audit.ts:178). Net: an apca-lane cta hue rotation or chroma change (precisely the axes C12 machinery moves — solveBrandExit hue exits, red repel, vivid gates) with L held ships past every snapshot gate. onFill pole booleans (the '6 apca-dark on-cta flips' class) are likewise un-snapshotted anywhere.

**Repro:** Add +15° to light cta H in the producer (L unchanged); `npm run highlight-audit` → snapshot 'clean'; `npm run audit` snapshot has no cta entries; visible brand-cta hue change ships with all snapshot gates green.

**Verify:** CONFIRMED, empirically. highlight-audit.ts:202-203 steps i+=3 over [L,C,H] triples but tests only v[i] (L) — proven by perturbing the blessed snapshot (all 408 triples: C+0.08, H+15°, L held) and running npm run highlight-audit: "clean — matches blessed", PASS, exit 0. No other snapshot covers ctas under the shipped apca profile: dark-audit's snapshot is stops slice(0,12) only (no cta roles, though it DOES use full OKLab ΔE); divergence-audit.ts:157 snapshots the 4 cta roles but resolves resolveBrand WITHOUT contrastProfile (default/wcag lane — build.ts passes 'apca' explicitly), so apca-lane cta solves (resolve.ts:106/135 branch on contrastProfile==='apca') are invisible to it, and its compare (line 180) checks only v[i] and v[i+1] — H uncompared even in its own lane; ext-override snapshot is path-shape only; smoothness baseline is ramp metrics, report-only. Net: an apca-lane cta hue rotation or chroma change with L held passes every snapshot gate; onFill pole booleans are value-snapshotted nowhere. One overstatement in the claim: highlight-RUNG C/H drift is NOT invisible for brands/accents/signals — dark-audit's full-ΔE snapshot covers stop 9 under apca (neutrals excepted). That detail aside, the core defect stands. Blessed snapshot restored byte-identical after the test.

## [MEDIUM] scripts/divergence-audit.ts:180 — Divergence snapshot ('the byte-identical guard') compares L and C but never H

**Mechanism:** matrix() (line 156-157) stores [L,C,H] per stop + all four cta roles, but the drift test checks `Math.abs(v[i]-r[i]) > TOL || Math.abs(v[i+1]-r[i+1]) > TOL` — v[i+2] (H) is never read. Header comment calls this 'the byte-identical guard for the cross-family consolidation'; a pure hue rotation at constant L,C on any stop or cta (red-cool scope widening, gold-spine torsion change, shear regression) reports 'clean — matches blessed'. Same bug class as the highlight snapshot one axis over — dark-audit's stopDeltaE-based compare (dark-audit.ts:211) shows the intended full-axis form. Also note this snapshot resolves without contrastProfile (line 161), so it guards the wcag lane only.

**Repro:** Rotate stop-5 H by 20° at constant L/C for one hue family; `npm run audit:divergence` → snapshot regression: clean; only dark-audit (different lane/population) might notice via stopDeltaE if the family is in its fleet.

**Verify:** Claim confirmed by direct code read. divergence-audit.ts:156-157 stores [L,C,H] triples per stop plus all four cta roles, but the drift loop (line 179-180) steps i+=3 and compares only v[i] (L) and v[i+1] (C) — v[i+2] (H) is never read, so a change confined to H indices mathematically cannot trigger drift; the repro (H-only rotation → 'clean — matches blessed') holds by construction. The header (lines 1-4) explicitly claims the snapshot guards 'the full family × mode × stop L/C/H matrix' as 'the byte-identical guard', and no comment documents an intentional H skip, so it is not intended behavior. No backstop in the script: Section B gates hue only for red-band signals vs seed hue (brands/neutrals/non-red signals uncovered); A is neutral chroma parity; C/D report-only. Side-claims also check out: snapshotOf (line 161) passes no contrastProfile and resolve.ts:41 shows undefined = default non-apca lane, so the apca lane is unguarded; dark-audit.ts:211 indeed uses full-axis stopDeltaE(L,C,H) — the intended form. Only refutation angle (H rotation always drags C past TOL 0.015) fails: hue-dominant engine transforms exist and mid-gamut re-clamp leaves C unchanged.

## Refuted (recorded so they are not re-found)

- p2Diff hexOf clamps in P3 then per-channel crushes to sRGB — hue-shifted rendition feeds the dark-cta membership/release metric
  - why refuted: Mechanism real but behavior intended, documented, and calibration-consistent — the claim's central premise is factually wrong. hexOf (p2.ts:24) does P3-clamp then per-channel-crush, and the repro reproduces (#f1007a vs #e7007a). But the claim asserts "her pages showed chroma-reduction-clamped hexes" — false: every calibration page uses a byte-identical encode (clampChromaToGamut default-P3 → oklchToLinearRgb → per-channel gm crush): c12-delivery-ladders.ts:51-57 (source of the 9 marks defining P2_D 0.12/P2_D_UP 0.11), c12-helmlab-research.ts:16-22 (the one-radius .116 fit), c12-gate-calibrate.ts:85-91, c12-dark-solve.ts:130-136 (the owner-approved F1+F2 exhibit). Probe over her actual ladder geometry (COLS × anchor±i·0.02, both modes): 219/320 swatches out-of-sRGB so the crush FIRED on her calibration pixels, 218 hexes differ between renditions, |Δp2| up to 0.0142 on marked colors. Her marks were made on crush-rendition pixels; p2.ts:21's comment is accurate as written, and the metric provably judges the SAME rendition its bars were fit to. The proposed one-line fix (srgb in hexOf) would de-calibrate: 0.0142 rendition delta on her marked ladders exceeds the 0.010 hysteresis, so bars would need re-derivation — the "fix" introduces the very metric-vs-calibration mismatch the claim alleges. P3-DESIGN's "never per-channel clamping" rule is explicitly scoped to the emit-boundary (cssRender toHex:8, rgba outline:206, figmaRender:120 — pixels users see), not judgment instruments. Claim itself concedes zero bar-flips on the shipped population. Residual observation (shipped srgbEmitChannels pixels differ from judged rendition by ≤0.0079, within her marks' own CV 12% noise) is a design tradeoff already resolved toward calibration fidelity, not a bug.

- On-fill enforcement assertion is white-pole-only, but the pipeline went pole-symmetric at 284f122 — black-pole enforcement regressions pass THE GATE
  - why refuted: Repro's load-bearing evidence clause is empirically false, and the proposed fix is a no-op on this gate — the qualifier is not the cause of any missed detection. What's true: scripts/reqtoken-audit.ts:116 does gate 'on-fill-enforce' on r.ons.onFillIsWhite, the line 110-111 comment is stale (284f122 touched src/reqtoken/producers.ts + scripts/dark-audit.ts but not reqtoken-audit.ts), producers ctaLightLApca/ctaDarkEnforcedLApca are now pole-symmetric, and reverting their black branches to the pre-284f122 early-return shape does yield 'GATE: PASS, failures: 0' (I ran this repro live: patched, ran npm run req:audit, PASS, restored via git checkout — tree back to byte-identical, gate green). BUT the claim's clause 'PASS despite black-pole seeds reading Lc < 60 with unmoved fills' does not hold: I instrumented the identical sweep (24 hues x 3 chromas x 2 modes, apca profile, the audit's own hex-based apcaY/apcaLc) under the regressed producers and found ZERO black-pole ctas — all 144 apca seed x mode ctas judge onFillIsWhite=true (SEED_L is fixed at 0.62, reqtoken-audit.ts:17, and every cta anchor at that grid prefers white). 0 seeds under Lc 59.9, 0 even inside the Lc<62 fire band. So the PASS is vacuously correct, not a masked violation; the onFillIsWhite qualifier's suppressing branch is unreachable at this gate's grid. Consequently the proposed fix (drop the qualifier: 'chosen pole under bar + !cta.enforced = fail either pole') would ALSO pass the stated repro — it detects nothing the current check misses, so the repro cannot distinguish fixed from unfixed gate. The real blind spot for black-pole enforcement is the sweep GRID (fixed seed L never produces a black-pole cta; the pre-284f122 Lc-54.7 greens lived in the named fleet, caught today by dark-audit F, now HARD exit-1 per 284f122 — dark-audit.ts iterates BRANDS+SIGNALS via engine resolveBrand, matching the claim's own fleet-backstop concession). Residual truths worth a separate, differently-scoped task: the stale comment at reqtoken-audit.ts:110-111, and the grid gap (black-pole coverage needs a seed-L dimension, e.g. sweeping SEED_L, not a qualifier drop — dropping the qualifier alone is dead code at the current grid). Bug-as-diagnosed (line 116 qualifier = the regression hole, with that repro as proof) is refuted.
