# Archive — HISTORICAL, do not ground decisions here

These are the stage1–10 handoffs and dark-mode explorations from the engine-spec effort. They
captured the journey and **contradict each other**. They are kept only for provenance.

**The single source of truth is [`../ENGINE-SPEC.md`](../ENGINE-SPEC.md).** Do not ground any
engineering or design decision in the files here — read ENGINE-SPEC instead. If you need detail
that only lives here (e.g. the full per-mechanism classification in `stage1-code-truth.md` /
`stage2-adjudication.md`), treat it as background, and reconcile against ENGINE-SPEC §3.
