# C12 — the two-problem split (owner, 2026-07-10, ends the calibration round)

## Her words (verbatim)
> It's not an individual rule so i shouldn't be marking dark. And to be fair, this has been
> hard because I have been having a hard time separating "does this look like an error color"
> from "do these two color vibrate against each other" which is maybe because these are 2
> problems and i should have been looking at moving the red for the vibrant colors.
>
> The scope is just blowing up and I don't want to keep spinning. I can let you do a deep
> session to propose an idea based on all the markings you have been given but before we do I
> think we would need to differentiate those 2 things "what looks error" vs "what looks too
> visually similar"

## The two problems
- **P1 — ERROR-LOOK**: does this color, alone and at a glance, read as the destructive/error
  signal? Identity/memory judgment. Governed by her fitted gate (v7: wDark .70 · wLight 1.6 ·
  wDust 1.4 · wGold 1.6 · G .090 — 0/100 across two sessions).
- **P2 — VISUAL SIMILARITY / VIBRATION**: do these two colors, adjacent in a real layout,
  sit too close / shimmer against each other? Proximity discriminability judgment.

## The data already proves they're distinct
4 of her 9 delivery-ladder marks ("first rung truly different", flush side-by-side pairs) sit
INSIDE the fire range her own P1 gate defines (metric 0.061–0.082 < G 0.090): H36 C0.14 both
directions, H44 C0.17 up, H16 C0.18 down. Same pair simultaneously "truly different" (P2,
side-by-side) and "confusable with the signal" (P1, at a glance). One judgment cannot produce
both → two problems. The flush-strip format (her layout ask) accidentally built a P2
instrument; the earlier separated-pair calibrations were P1 instruments.

Where the ladder marks are LARGE (vivid warm: up +0.10 / down −0.18) is where P1 and P2
overlap — vibrant near-reds are both error-look-alike and vibration-prone.

## Her design instinct on record
"i should have been looking at moving the red for the vibrant colors" — for vibrant near-red
brands, move the SIGNAL (per-brand red variant), not the brand. NOTE: this is the owner-settled
PARKED C12 design resurfacing (catalog: brand→bright + red→RICH #a82100 per-brand variant,
lemon-swap family) — machinery exists (signalOverrides; yellow/green/info variants shipped).

## Dark
"It's not an individual rule so I shouldn't be marking dark" — correct per the delta model:
dark derives (dark = live fn of light; cta = prominence-floored anchor). The dark repel is
DERIVED + VERIFIED (floor/bar/exhibits on dark bg), never hand-calibrated.

## Status
Calibration rounds CLOSED at her word ("don't want to keep spinning"). Next = deep proposal
session over all accumulated marks, gated on her confirming this P1/P2 split.
